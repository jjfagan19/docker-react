sudo: required
language: generic
services:
    - docker

before_install:
    - docker build -t jjfagan19/docker-react -f Dockerfile.dev . 

script:
    - docker run -e CI=true jjfagan19/docker-react npm run test -- --coverage
